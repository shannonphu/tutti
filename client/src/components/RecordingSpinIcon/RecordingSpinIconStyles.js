export default (theme) => ({
    root: {
        paddingTop: 5.5,
        paddingLeft: 0
    },
    progress: {
        position: 'absolute',
        top: 17.5,
        left: 12,
        zIndex: 1,
        color: theme.palette.error.main
    }
});