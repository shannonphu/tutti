export default (theme) => ({
    root: {
        paddingRight: 5
    },
    progress: {
        position: 'absolute',
        top: 12,
        left: 16,
        zIndex: 1,
        color: theme.palette.error.main
    }
});